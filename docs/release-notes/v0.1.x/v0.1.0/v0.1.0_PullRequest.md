# PR Draft — v0.1.0 Release Prep (DISABLED)

> DISABLED (2026-05-28): repo-management guidance temporarily deactivated. Draft preserved below.

<!--

## Title
Release v0.1.0: documentation + tooling alignment

## Summary
Prepares the repository for the v0.1.0 release by aligning documentation and tooling with the canonical `src/` layout, fixing broken links, and correcting VS Code config.

## Key changes
- Changelog: add a v0.1.0 section and normalize older path references
- Docs: standardize naming and update architecture/runbook paths
- Tooling: fix `.vscode/` JSON and update PAC task output locations
- GitHub health files: add `.github/CONTRIBUTING.md`, `.github/SECURITY.md`, `.github/CODE_OF_CONDUCT.md` as pointers to the canonical docs

## Validation
- Opened and spot-checked README/changelog links in VS Code
- Verified `.vscode/` JSON parses cleanly
- Optional (requires `pac` auth): run VS Code tasks
  - Canvas: Unpack .msapp to src
  - Solution: Unpack zip to src

## Notes
- `dist/`, `tmp/`, `archive/`, and `docs/local/` remain local-only (git-ignored).

## Checklist
- [ ] CHANGELOG updated
- [ ] Docs updated (README / runbook / architecture)
- [ ] Environment-specific values reviewed/sanitized
- [ ] Ready to tag `v0.1.0`
-->
