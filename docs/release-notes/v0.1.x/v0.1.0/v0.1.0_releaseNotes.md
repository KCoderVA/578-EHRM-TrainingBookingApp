# Release Notes — v0.1.0 (DISABLED)

> DISABLED (2026-05-28): repo-management guidance temporarily deactivated. Draft preserved below.

<!--

- Tag: v0.1.0
- Date: 2026-05-27

## Highlights
- Power Automate `AppUserList` updated to v0.1.0 (naming/notifications + minor behavior tuning)
- Documentation and repo tooling standardized to match the canonical `src/` layout
- VS Code config fixed (valid JSON) and PAC tasks updated to target the correct folders

## Changes
See [CHANGELOG.md](../../CHANGELOG.md) for the full entry.

### Added
- Release prep draft files under `docs/release-notes/`

### Changed
- Power Automate: `src/powerAutomate/AppUserList/` updated to v0.1.0
- VS Code tasks: Canvas pack/unpack uses `src/powerApps/.unpacked/`; solution unpack uses `src/config/solutions/EHRMTrainingBooking/`
- Docs: corrected SharePoint sample data paths (now under `src/sharePoint/`) and updated architecture/runbook references

### Fixed
- Broken links and stale path references
- Invalid JSON in `.vscode/` workspace/config files

## Upgrade Notes
- Export artifacts remain local-only under `dist/release/`.
- Recommended refresh steps (requires `pac` auth):
  - Export/refresh solution zip → unpack to `src/config/solutions/EHRMTrainingBooking/`
  - Export/refresh canvas `.msapp` → unpack to `src/powerApps/.unpacked/`
- Review/sanitize unpacked sources for environment-specific IDs/URLs/emails before broad sharing.

## Assets (optional)
- Attach `dist/release/EHRMTrainingBooking_Solution.zip`
- Attach `dist/release/EHRMTrainingBookingApp.msapp` (or the exported `.msapp` used for packing/unpacking)
-->
