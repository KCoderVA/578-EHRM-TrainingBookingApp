# PR: v0.1.1 — public-release documentation + repo alignment

## Title

`chore(release): v0.1.1 public-release documentation + repo alignment`

## Summary

This PR prepares the **EHRM Training & Booking App** repo for the **v0.1.1** public release by:

- Replacing placeholder v0.1.1 release drafts copied from an unrelated project
- Updating the primary public-facing docs (README + CHANGELOG)
- Adding a single “project status / release readiness” doc
- Aligning repo documentation and ALM tasks to the current canonical folder layout

No changes are intended to the underlying Canvas app / Power Automate logic in this PR; this is primarily a documentation + repository hygiene release.

## Changes at a glance

| Area | What changed |
|------|--------------|
| Release drafts | Rewrote v0.1.1 commit/PR/release notes under `docs/release-notes/v0.1.x/v0.1.1/` |
| Public entry docs | Updated `README.md` and `CHANGELOG.md` for v0.1.1 |
| Status doc | Added `docs/PROJECT_STATUS.md` |
| Runbooks | Canonical architecture + ALM runbook locations under `config/` |
| GitHub readiness | Enabled/updated `.github` community templates (SECURITY/CONTRIBUTING/PR/issues) |
| ALM tasks | Updated `.vscode/tasks.json` paths to match `config/solutions/...` |

## Key files

- Release docs: `docs/release-notes/v0.1.x/v0.1.1/`
- Changelog: `CHANGELOG.md`
- README: `README.md`
- Status: `docs/PROJECT_STATUS.md`
- Architecture: `config/architecture/ARCHITECTURE.md`
- ALM runbook: `config/runbooks/ALM-RUNBOOK.md`

## Verification

- [ ] Read through the v0.1.1 drafts and confirm content matches the intended release narrative
- [ ] Confirm README links resolve correctly
- [ ] Confirm `CHANGELOG.md` renders correctly
- [ ] Confirm `.vscode/tasks.json` unpack targets match your current workspace layout

## Repo alignment: tracked deletions (expected)

The following paths are deleted locally and should be removed from the repo on merge (they have newer canonical replacements):

- `CONTRIBUTORS.md` (replaced by `docs/CONTRIBUTORS.md`)
- `SECURITY.md` (policy lives under `.github/SECURITY.md` and `docs/SECURITY.md`)
- `docs/architecture/ARCHITECTURE.md` (replaced by `config/architecture/ARCHITECTURE.md`)
- `docs/runbooks/ALM-RUNBOOK.md` (replaced by `config/runbooks/ALM-RUNBOOK.md`)
- `src/config/tools/pac/alm.ps1` (replaced by `config/tools/pac/alm.ps1`)

## Release notes (post-merge)

After merge:

1. Tag: `v0.1.1`
2. Create GitHub Release using `docs/release-notes/v0.1.x/v0.1.1/v0.1.1_releaseNotes.md`
3. (Optional) Attach export artifacts from local `dist/release/` (Solution `.zip` and/or `.msapp`)
