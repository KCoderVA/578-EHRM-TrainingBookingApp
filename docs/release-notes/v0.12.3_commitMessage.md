feat(powerAutomate): v0.12.3 — add SendReminders reminder-email flow, re-export AppUserList, refresh docs

## Context

This commit releases the **578-EHRM-TrainingSchedulerApp** repo at **v0.12.3**, a Power Automate-only patch that lands the flow work intentionally deferred from the v0.12.2 go-live release. The flows were built/exported in Power Automate and unpacked into `src/powerAutomate/`; there are **no Canvas app changes** (the Canvas app component remains v0.12.2).

---

## What changed — Power Automate (`src/powerAutomate/`)

### New flow: `SendReminders` ("Send Email Reminder")
- `SendReminders/.unpacked/Microsoft.Flow/flows/ecd1d899-5df4-4795-a98b-f1a1b55ee837/` — NEW unpacked flow source (`definition.json`, `apisMap.json`, `connectionsMap.json`, manifests).
- `SendReminders/adaptiveCards/v0.12.4_flowSendReminders_adaptiveCard.json` — NEW Adaptive Card email body ("You are scheduled for EHRM Learning Lab training!") that pulls name, email, role, scheduled-by, and access window from `DeskAccessControl`.
- `SendReminders/.zip/v0.12.2_578EHRMTrainingApp_SendEmailReminder_20260812115737.zip` — packed export (local-only; `.zip` git-ignored).
- `SendReminders/README.md` — NEW subcomponent README.
- Fulfills the previously-tracked roadmap item to build the reminder Power Automate flow.

### `AppUserList` (re-export, v0.1.0 → v0.1.1)
- `AppUserList/.unpacked/.../{apisMap,connectionsMap,definition}.json` + `manifest.json` — connection/API/manifest maps refreshed.
- `AppUserList/.json/578EHRMTrainingApp_AppUserList.json` → `AppUserList/.json/v0.12.2_578EHRMTrainingApp_AppUserList.json` — export renamed with version prefix.
- `AppUserList/README.md` — updated for the re-export.

---

## What changed — project documentation

### Updated files
- `VERSION` — 0.12.2 → 0.12.3
- `README.md` — release badge → v0.12.3; "Current versions" now lists `SendReminders`
- `CHANGELOG.md` — added `[0.12.3]` entry (Added, Changed, Notes)
- `docs/PROJECT_STATUS.md` — status summary, component versions, release-history row, roadmap (reminder flow now marked built)
- `src/powerAutomate/README.md` — documented both flows + component-version table

### New files
- `docs/release-notes/v0.12.3_commitMessage.md`, `docs/release-notes/v0.12.3_pullRequest.md`, `docs/release-notes/v0.12.3_releaseNotes.md`
- `src/powerAutomate/SendReminders/README.md`
- `assets/images/screenshots/2026.08.12_confirmationEmailChangeRequest.png` — AppUserList access-change email screenshot

### Moved / archived files
- `v0.12.4_flowSendReminders_adaptiveCard.json` (repo root) → `src/powerAutomate/SendReminders/adaptiveCards/` — tidy root; colocate with the flow
- `v0.12.2_flowSendReminders_adaptiveCard.json` (repo root) → `archive/superseded/2026-08-20/` (local-only) — superseded card design

### Rotated release-note artifacts
- `docs/release-notes/v0.12.2_{commitMessage,pullRequest,releaseNotes}.md` — removed (archived locally under `archive/` by the post-commit archival step); replaced by the `v0.12.3_*` set above.

---

## Excluded (intentional)
- Unrelated in-progress drift (powerApps HTML scratch files, future-version screenshots) is **not** part of this commit.
